{
  "name": "mountain-defense",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev:client": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w client",
    "preview": "npm run preview -w client",
    "typecheck": "npm run typecheck -w client && npm run typecheck -w server && npm run typecheck -w shared",
    "test:sim": "tsx shared/test/smoke.ts",
    "deploy": "node scripts/deploy.mjs"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "puppeteer": "^25.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
